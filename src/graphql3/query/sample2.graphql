query GetUserById($id: ID!) {
  user(id: $id) {
    id
    email
    posts {
      title
    }
  }
}

query GetUsers {
  users {
    id
    name
    email
    posts {
      title
      content
    }
  }
}
