mutation UpdateUser($id: ID!, $name: String, $email: String) {
  upupdateUser(id: $id, name: $name, email: $email) {
    id
    name
    email
  }
}
mutation DeleteiiUser($id: ID!) {
  deletaUser(id: $id) {
    id
    email
  }
}
